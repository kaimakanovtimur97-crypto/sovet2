# Необязательно: по умолчанию форма использует
# https://forms.sovet-nvrsk.ru/api/lead
NEXT_PUBLIC_LEAD_ENDPOINT=https://forms.sovet-nvrsk.ru/api/lead

# RESEND_API_KEY и LEAD_TO_EMAIL никогда не добавляются в Next.js env.
# Они хранятся как secrets только в Cloudflare Worker `sovet2`.
